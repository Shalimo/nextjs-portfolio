import styles from "./Main.module.scss"

const Main = ({projects}) => {
    return(
        <section className={styles.section}>
            <div>Home</div>
        </section>
    )
}

export default Main